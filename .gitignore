node_modules
dist
data/raw/
.DS_Store
.vite
*.log
.claude/

# If the build fetches data (see the dataviz-pages-site skill's build-time-data
# reference), keep the fetched files out of git and let CI regenerate them:
# public/data/
